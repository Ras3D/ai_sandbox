import React from "react"

function Header(){
    return (
        <header>
            <h1>CHAT GPT-4 Translate</h1>
            <p>React version{" "}{React.version}</p>
        </header>
    )
}

export default Header